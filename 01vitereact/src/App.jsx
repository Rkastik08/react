import Index from './Index.jsx'
function App() {
  

  return (
  <Index/> //jsx requires only one element to be returned. so we can use <div> but instead of writing div again and again we can write FRAGMENTS <> </> this.
  )
}

export default App
